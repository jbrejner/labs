version: 1
trail:
  artifacts:
    - name: application
      attestations:
        - name: unit-tests
          type: junit
    - name: docker-image
      attestations:
        - name: sbom
          required: generic
        - name: performance-test  # We haven't implemented this yet!
          required: generic
